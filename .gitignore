# Build output — generated in the build-publish pipeline, never committed.
dist/

# Node
node_modules/

# OS / editor cruft
.DS_Store

# Private local notes (persona/identity setup) — never publish
.claude/

# Other AI assistant / editor local dirs — never publish
.cursor/
.windsurf/
.continue/
.codeium/
.gemini/
.cline/
.roo/
.aider*
.qodo/
