#!/usr/bin/env bash
# Build the static site from data/ into dist/. Runs in the manual build-publish pipeline.
set -euo pipefail
cd "$(dirname "$0")/.."
npm install --no-audit --no-fund --silent
node scripts/generate.mjs
