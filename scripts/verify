#!/usr/bin/env bash
# Validate the catalog data. Runs automatically on push (verify-data pipeline).
set -euo pipefail
cd "$(dirname "$0")/.."
npm install --no-audit --no-fund --silent
node scripts/verify.mjs
